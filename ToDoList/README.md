🌱 新手完整项目：命令行待办事项 To-Do List

学完这个项目你能掌握：

变量、列表

if / while

函数

文件读写（非常关键）

写出一个“能长期用”的小程序

📌 项目目标

你将做出一个程序，可以：

查看待办事项

添加待办事项

删除待办事项

退出程序

关闭后再次打开，任务还在（文件保存）
✅ 第 0 步：准备环境（1 分钟）

你需要：

已安装 Python（3.8+）

一个 .py 文件，比如：todo.py

🧠 第 1 步：程序主菜单（先跑起来
    · 熟悉if else 的写法，while 的写法
    · 变量的创建及赋值
    · print(), input()的写法

🧠 第 2 步：创建待办事项列表
    · 熟悉for in 遍历读取数据
    · 了解enumerate的用法：在遍历列表时，同时拿到“索引 + 元素，类似于js中的foreach
    · 了解append方法：类似于js中的push

🧠 第 3 步：实现删除功能（很重要）
    · 了解isdigit()方法：字符串自带的方法，用来判断字符串是不是“纯数字”
    · 了解len()方法：用来返回“一个容器里有多少个元素”，类似于js中的数组长度

🧠 第 4 步：保存到文件（程序升级关键点）
    | 部分          | 含义            |
| ----------- | ------------- |
| `open(...)` | 打开文件          |
| `with`      | 用完自动帮你关文件     |
| `as f`      | 给打开的文件取名叫 `f` |
| `def`       | 定义函数             ｜
|`line.strip()` | 去掉字符串两端的空白字符|

| 模式    | 作用 | 文件不存在时       |
| ----- | -- | ------------ |
| `"r"` | 读取 | ❌ 报错         |
| `"w"` | 写入 | ✅ **自动新建文件** |
| `"a"` | 追加 | ✅ 自动新建文件     |



